<br/>

<table summary ="collection view">
	<tr>
		<% @collection.each_with_index do |collection, index|%>
		<th> <%= link_to image_tag(collection.photo.url(:medium)), {:action => detail, :id => collection.id}%> <br/>
			<%= collection.name %>
			<% if (index+1)%4==0 %>
		</th> </tr>			
			<% end %>		
		<%end%>
	</th> 
</tr>
</table>