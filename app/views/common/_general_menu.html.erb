<div id="top">
	<div class="navbar navbar-inverse">
		<div class="navbar-inner">
			<!-- Be sure to leave the brand out there if you want it shown -->
			<%= link_to('LittleBlueBinder', {:action => 'browse_collection', :controller => 'collection'}, {:class => "brand"})%>

			<% if vendor_authorization?%>

			<ul class="nav pull-right">
				<%if current_page?(:action => 'view_items') %> 
				<li class="active">
					<%else%>
					<li>
						<%end%>
						<%= link_to("Home", :action => 'view_items', :controller => 'vendor') %>		
					</li>
					<li class="divider-vertical"></li>
					<li>
						<%= link_to("Dashboard", :action => 'dashboard', :controller => 'vendor') %>		
					</li>
					<li class="divider-vertical"></li>
					<li>
						<%= link_to("Log out", :action => 'logout', :controller => 'access') %>		
					</li>
				</ul>


				<% elsif logged_in?%>
				<ul class="nav pull-right">
					<%if current_page?(:action => 'view_collection')%> 
					<li class="active">
						<%else%>
						<li>
							<%end%>
							<%= link_to("Home", :action => 'view_collection', :controller => 'collection') %>		
						</li>
					  <li class="divider-vertical"></li>
					<%if current_page?(:controller => 'visualize')%> 
					<li class="active">
						<%else%>
						<li>
							<%end%>
							<%= link_to("Visualize", :action => 'index', :controller => 'visualize') %>		
						</li>


					  <li class="divider-vertical"></li>

						<li class="dropdown">
							<a class="dropdown-toggle" data-toggle="dropdown" href="#">
								Upload
								<span class="caret"></span>
							</a>
							<div class="dropdown-menu" role="menu" aria-labelledby="dLabel">
								<%= link_to({:action => 'upload_scrap', :controller => 'scrap'}) do%>
								<i class="icon-picture"></i> Scrap
								<%end%>
							</div>
						</li>
					  <li class="divider-vertical"></li>

						<li class="dropdown">
							<a class="dropdown-toggle" data-toggle="dropdown" href="#">
								Account
								<span class="caret"></span>

							</a>
							<div class="dropdown-menu pull-right">
								<%= link_to({:action => 'profile', :controller => 'access'}) do%>
								<i class="icon-user"></i> Profile
								<%end%>
								<%= link_to({:action => 'logout', :controller => 'access'}) do%>
								<i class="icon-off"></i> Logout
								<%end%>
							</div>
						</li>

					</ul>

					<%else%>
					<ul class="nav pull-right">
						<li>
							<%= link_to("Sign Up", :action => 'signup', :controller => 'access') %>		
						</li>
						<li class="divider-vertical"></li>
					  
						<li>
							<%= link_to("Log In", :action => 'login', :controller => 'access') %>		
						</li>
					</ul>		
					<%end%>
				</div>

			</div>
		</div>
		<br><br>