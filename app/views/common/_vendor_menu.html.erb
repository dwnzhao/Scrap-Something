<div id="tool_menu" >
	<%if session[:user_id] != nil%>
	<div class="btn-group btn-toolbar">
		<div class="dropdown btn btn-inverse btn-mini">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#">
				Account
				<span class="caret"></span>

			</a>
			<div class="dropdown-menu pull-right">
				<%= link_to({:action => 'vendor_profile', :controller => 'vendor'}) do%>
				<i class="icon-user"></i> Profile
				<%end%>
				<%= link_to({:action => 'logout', :controller => 'access'}) do%>
				<i class="icon-off"></i> Logout
				<%end%>
			</div>
		</div>
	</div>
	<%end%>
</div>

<div id= "home_menu">	
	<%= link_to_unless_current("Organize Items", :action => 'view_items', :controller => 'vendor') do %>
	<i class="icon-home icon-white"></i>
	Organize Items
	<%end%>
	<%= link_to_unless_current("Dashboard", :action => 'dashboard', :controller => 'vendor') do%>
	<i class="icon-globe icon-white"></i>
	Dashboard
	<%end%>

</div>